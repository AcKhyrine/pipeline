# name: "Surgical Code Move (Cherry-Pick Style)"
# on:
#   workflow_dispatch:
#     inputs:
#       proxy_name:
#         description: 'Name of the proxy folder to move'
#         required: true
#       target_branch:
#         description: 'Branch to move code to (uat or main)'
#         required: true
#         default: 'uat'

# jobs:
#   move-code:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Target Branch (UAT/PROD)
#         uses: actions/checkout@v4
#         with:
#           ref: ${{ github.event.inputs.target_branch 
#           fetch-depth: 0

#       - name: Configure Git
#         run: |
#           git config user.name "Robot-Promoter"
#           git config user.email "actions@github.com"

#       - name: "Surgically Sync Proxy Code"
#         run: |
#           PROXY="${{ github.event.inputs.proxy_name "
          
#           # This is the "magic" command. 
#           # It reaches into the 'sit' branch and grabs ONLY that folder.
#           git checkout origin/sit -- src/main/apiproxies/$PROXY
          
#           git add .
          
#           # Only commit if there are actually changes
#           if git diff-index --quiet HEAD; then
#             echo "No changes to move for $PROXY"
#           else
#             git commit -m "Surgical promotion of $PROXY from SIT"
#             git push origin ${{ github.event.inputs.target_branch 
#           fi

#       - name: "Run Deployment Logic"
#         run: |
#           echo "Now that the code is merged, I can trigger the API promotion..."
#           # Your CLI command to deploy the highest revision